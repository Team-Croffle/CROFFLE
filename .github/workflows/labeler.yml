name: Pull Request Labeler

on:
  pull_request_target:
    types: [opened, reopened, synchronize]

jobs:
  label:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      # Allows creating labels that do not exist yet
      issues: write
    steps:
      - uses: actions/labeler@v5
        with:
          # Repo uses .yaml (action default is labeler.yml)
          configuration-path: .github/labeler.yaml
          sync-labels: true
